groups:
- name: drift_alerts
  rules:
  - alert: F1ScoreTooLow
    expr: weighted_f1_score < 60
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: "Weighted F1 Score unter 60 %"
      description: "Der weighted F1 Score deiner Prediction-API ist seit über 5 Minuten unter 60 %: aktueller Wert {{ $value }} %."
